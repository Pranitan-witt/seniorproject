<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%- title%></title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" type="text/css" href="holder.css" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
  </head>
  <body>
    <nav class="navbar navbar-expand-sm bg-info navbar-dark">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="#">PRONHUB</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">THAI</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">HANIME</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">DOJIN</a>
        </li>
      </ul>
    </nav>
    <script type="text/javascript" src="holder.js"></script>
    <div class="container-fluid h-100">
      <div class="row justify-content-center h-100">
        <div id="menu" class="col-sm-4">
            <p id="menu-topic">WALLET</p>
            <div id="name-list">                
                <% for(var i=0;i<list.length;i++) { %>
                    <a class="list" id="name" href="#"><%= list[i] %></a><br />
                <% } %>
          </div>
        </div>
        <div id="content" class="col-sm-8">
          <p id="content-topic">HOLDER</p>
          <div id="content-box">
            <p>

              <% selectedData = null; %>
              <% if (selectedData != null) {%>
                <% for(var i=0;i<list.length;i++) { %>
                  <%= mock_up_data[i] %><br>
                <% } %>
              <% } %>
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- <script>
      var x = document.getElementById("name");
      x.addEventListener("click", test);

      function test() {
        selectedData =
      }
    </script>   -->

  </body>
</html>
